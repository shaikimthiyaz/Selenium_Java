# what is class?

A class is a **blueprint** or **template** used to create objects.

👉 It defines:

Data (variables)

Behavior (methods)

### Real-Life Example

Think of a House Blueprint:

It defines number of rooms, doors, windows

But it is not a real house

Similarly:

Class = Design

Object = Real thingx


### java class example
```
class Student {
    String name;
    int age;

    void study() {
        System.out.println("Student is studying");
    }
}
```

🔹 Student → class
🔹 name, age → variables
🔹 study() → method

# What is an Object?

An object is an instance of a class.

It’s the actual entity created from the blueprint.

Each object has its own state & behaviour

Example:

```
public class Main {
    public static void main(String[] args) {

        Student s1 = new Student();
        s1.name = "Rahul";
        s1.age = 20;
        s1.study();

        Student s2 = new Student();
        s2.name = "Anita";
        s2.age = 22;
        s2.study();
    }
}
```

### HOW CLASS & OBJECT WORK INTERNALLY 

When this line executes:

Student s1 = new Student();


👉 Java does:

Allocates memory

Creates object

Assigns reference s1

Calls constructor


## class vs object

| Class             | Object            |
| ----------------- | ----------------- |
| Blueprint         | Real instance     |
| Logical           | Physical          |
| No memory         | Takes memory      |
| Defines structure | Holds actual data |

# What is a Constructor?

A constructor is a special method:

Same name as class

No return type

Called automatically when object is created

## Why Constructors Are Needed (Real Life)

Imagine opening a bank account:

Account number

Name

Initial balance

Without initialization → account is useless.

### Example: Without Constructor 
```
class Student {
    String name;
    int age;
}
```

👉 Object is created, but values are empty or 0.


### 1.with constructor (Parameterized Constructor)

Accepts arguments to initialize fields with specific values.

```
class Student {
    String name;
    int age;

    Student(String n, int a) {
        name = n;
        age = a;
    }

    void display() {
        System.out.println(name + " " + age);
    }
}


public class Main {
    public static void main(String[] args) {
        Student s1 = new Student("Rahul", 20);
        s1.display();
    }
}

```

### 2. Default Constructor

Provided by Java if you don’t define any constructor.

Initializes fields with default values (0, null, false).

**ex**:
```
class Student {
    String name;
    int age;
}

public class Main {
    public static void main(String[] args) {
        Student s = new Student(); // default constructor
        System.out.println(s.name); // null
        System.out.println(s.age);  // 0
    }
}
```

## static vs non static

| Static           | Non-Static        |
| ---------------- | ----------------- |
| Belongs to class | Belongs to object |
| One copy         | Multiple copies   |
| No object needed | Object required   |
| Loaded once      | Loaded per object |





